server:
  port: 8090

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/postgres?currentSchema=spring-final
    username: postgres
    password: postgres

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true